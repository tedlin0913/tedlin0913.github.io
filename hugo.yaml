baseURL: 'https://www.tedlintech.com/'
languageCode: 'en-us'
title: 'Ted Lin'
theme: "PaperMod"

minify:
  disableXML: true
  minifyOutput: true

params:
  profileMode:
    enabled: true
    title: "Ted Lin" # optional default will be site title
    subtitle: ""
    # imageUrl: "<image link>" # optional
    # imageTitle: "<title of image as alt>" # optional
    imageWidth: 120 # custom size
    imageHeight: 120 # custom size
    buttons:
      - name: Archive
        url: "/archive"
      - name: Github
        url: "https://github.com/tedlin0913/"